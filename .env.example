# runtime deps
npm install express @qdrant/js-client-rest

# dev types + typescript
npm install -D typescript @types/node @types/express

# if Node < 18 or you explicitly use node-fetch
# npm install node-fetch
# npm install -D @types/node-fetch

# quick TS check
npx tsc --noEmit

# runtime / secrets template
DATABASE_URL=your_database_url_here
JWT_SECRET=your_jwt_secret_here
CLOUD_STORAGE_BUCKET=your_cloud_storage_bucket_here
API_KEY=your_api_key_here
NODE_ENV=development

# === LLM CONFIGURATION ===
# IMPORTANT: You MUST choose ONE of the options below:

# OPTION 1: Claude Haiku API (RECOMMENDED - $0.80/1M tokens, ~$0.25/month)
# 1. Get your key from: https://console.anthropic.com/account/keys
# 2. Paste it here:
LLM_API_KEY=sk-ant-... (paste your Anthropic API key here)
LLM_CHAT_MODEL=claude-3-5-haiku-20241022
LLM_CHAT_ENDPOINT=https://api.anthropic.com/v1/messages

# OPTION 2: OpenAI API (Cheaper but lower quality - $0.15/1M tokens, ~$0.05/month)
# Uncomment these lines if you want to use OpenAI instead:
# 1. Get your key from: https://platform.openai.com/api-keys
# 2. Uncomment and paste it:
# LLM_API_KEY=sk-proj-... (your OpenAI key)
# LLM_CHAT_MODEL=gpt-4o-mini
# LLM_CHAT_ENDPOINT=https://api.openai.com/v1/chat/completions

# Common settings
LLM_TEMPERATURE=0.2
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_ENDPOINT=

QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=interview_context
EMBEDDING_DIM=1536